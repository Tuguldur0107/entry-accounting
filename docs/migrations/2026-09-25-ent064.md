# 2026-09-25 — ENT-064 PO дутуу хаалтын схемийн өөрчлөлт (up / down)

Deploy нь `npm run db:predeploy` (drizzle push) — доорх **up** нь автоматаар
хэрэгжинэ (шинэ багана, default-тай — одоо байгаа мөр хөндөгдөхгүй). **down**
нь буцаах шаардлагатай үед гараар ажиллуулах SQL. Кодыг өмнөх хувилбар руу
буцаасны ДАРАА ажиллуулна — эс бөгөөс push багануудыг дахин нэмнэ.

| # | Багана | up | down |
|---|--------|----|------|
| 1 | Цуцалсан тоо | `alter table purchase_order_lines add column cancelled_quantity numeric(18,4) not null default '0'` | `alter table purchase_order_lines drop column cancelled_quantity;` |
| 2 | Дутуу хаалтын шалтгаан | `alter table purchase_orders add column short_close_reason text` | `alter table purchase_orders drop column short_close_reason;` |

⚠️ down-оос ӨМНӨ дутуу хаагдсан захиалгуудыг (`short_close_reason is not null`)
дахин нээх (`reopenPurchaseOrder`) хэрэгтэй — эс бөгөөс хуучин код тэдгээрийг
«бүрэн хаагдсан» гэж харуулж, цуцалсан тоо ба шалтгаан алдагдана. GL
(хаалтын журнал) хөндөгдөхгүй.
