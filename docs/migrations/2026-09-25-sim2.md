# 2026-09-25 — Сим 2.0 засварын схемийн өөрчлөлт (up / down)

Deploy нь `npm run db:predeploy` (drizzle push) — доорх **up** нь автоматаар
хэрэгжинэ. **down** нь буцаах шаардлагатай үед гараар ажиллуулах SQL (өгөгдөл
алдагдахгүй: зөвхөн шинэ багана/мөр). Кодыг өмнөх хувилбар руу буцаасны ДАРАА
ажиллуулна — эс бөгөөс push багануудыг дахин нэмнэ.

| # | SIM2 | up | down |
|---|------|----|------|
| 1 | 023 | `alter table costing_account_settings add column open_po_close_mode text not null default 'block'` | `alter table costing_account_settings drop column open_po_close_mode;` |
| 2 | 038 | `alter table company_settings add column control_account_guard text not null default 'warn'` | `alter table company_settings drop column control_account_guard;` |
