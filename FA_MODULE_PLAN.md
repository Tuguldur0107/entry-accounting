# Үндсэн хөрөнгийн (FA) модулийн төлөвлөгөө

> Загвар: Inventory/Costing-ийн туршигдсан хэв — draft-first, advisory lock,
> цэвэр unit-тесттэй тооцоолол, эх үүсвэрээс автомат draft, GL тулгалт.
> KB: `ias-16-ppe.md`, gl-posting-matrix §2.20–2.25. ModuleKey `"fa"` бэлэн.

## 1. Хамрах хүрээ

| Үе | Багтах зүйл |
|----|-------------|
| **Үе 1 (одоо)** | Хөрөнгийн карт (бүртгэл, идэвхжүүлэлт) · **өглөгийн модултой уялдаа**: АП нэхэмжлэх/касс/GL-ээс 2Х-дансанд бичигдмэгц картын НООРОГ автоматаар үүснэ · шулуун шугамын сарын элэгдлийн run → ноорог → батлахад GL · NBV бүртгэл + GL тулгалт |
| Үе 2 | Данснаас хасалт (§2.25, олз 51800004/гарз 87100006), impairment (IAS 36), дахин үнэлгээ, IFRS 16 түрээс |

## 2. Данс, бичилт (KB matrix-аас)

```
Худалдан авалт (АП):  Dr 21010000 Үндсэн хөрөнгийн өртөг / Cr 31000001 АП
                      → АП модуль өөрөө бичнэ; FA модуль зөвхөн КАРТ нээнэ
Сарын элэгдэл (§2.21): Dr 70000001 Элэгдлийн зардал / Cr 21000099 Хуримт. элэгдэл
                      → FA модулийн ЦОРЫН ГАНЦ GL бичилт (Үе 1)
```

Картын default: өртгийн данс 21010000, хуримт. элэгдэл 21000099,
зардал 70000001 — карт бүр дээр өөрчилж болно.

## 3. Өгөгдөл

```
fixed_assets            — id, userId, code (unique/user, FA-... эсвэл гараар),
                          name, acquisitionDate, cost (MNT), salvageValue,
                          usefulLifeMonths, depreciationStartMonth (YYYY-MM),
                          assetAccountNumber / accumDepAccountNumber /
                          depExpenseAccountNumber, status (draft|active|disposed),
                          sourceVoucherId (худалдан авалтын GL), createdAt

fa_depreciation_entries — id, userId, assetId, periodMonth (YYYY-MM), amount,
                          status (draft|posted|reversed), voucherId,
                          reversalVoucherId, postedAt, createdAt
                          (asset+month 1 идэвхтэй бичилт — кодоор шахна)
```

## 4. Уялдаа (өглөгийн модуль + бусад суваг)

АП нэхэмжлэх батлагдах / касс / GL журнал **FA өртгийн данс** (21010000,
20000001, 21000001)-д цэвэр Dr бичмэгц → **картын ноорог** автоматаар үүснэ:
өртөг = тухайн воучерийн цэвэр Dr, огноо = воучерийн огноо, нэр = тайлбараас.
Хэрэглэгч ашиглалтын хугацаа, эхлэх сар зэргийг бөглөж **идэвхжүүлнэ** —
идэвхжүүлэлт GL бичихгүй (өртөг аль хэдийн данслагдсан). Идемпотент:
sourceVoucherId; элэгдлийн воучер өөрөө FA данс хөндөхгүй тул давхардахгүй.
Unpost/сторно → бөглөгдөөгүй ноорог карт автоматаар устна.

## 5. Элэгдлийн run

Сар сонгож "Элэгдэл тооцох": идэвхтэй, эхэлсэн, бүрэн элэгдээгүй карт бүрд
`min((өртөг − үлдэх өртөг)/хугацаа, үлдсэн суурь)` дүнтэй НООРОГ бичилт
(сард нэг л идэвхтэй бичилт). Батлахад карт бүрд `Dr зардал / Cr хуримт.
элэгдэл` воучер (S9="FA"), optimistic claim + advisory lock, сторно бүрэн
дэмжинэ. NBV = өртөг − Σposted элэгдэл; GL тулгалт: хуримт. элэгдлийн данс
vs Σposted бичилт, өртгийн данс vs Σ идэвхтэй картын өртөг.

## 6. UI (page-per-view)

```
Үндсэн хөрөнгө (/fa, icon: Building)
├── Хяналтын самбар   /fa               ← метрик (өртөг/хуримт.элэгдэл/NBV/ноорог) + NBV бүртгэл + GL тулгалт
├── Хөрөнгийн карт    /fa/assets        ← жагсаалт, шинэ/засах dialog, идэвхжүүлэх
└── Элэгдэл           /fa/depreciation  ← сар + run, ноорог батлах (batch) / сторно
```
